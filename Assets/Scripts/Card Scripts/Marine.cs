using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//cheap infantry unit that's effective against other infantry.
[CreateAssetMenu(menuName = "Card/Marine", fileName = "card_marine")]
public class Marine : CardData
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
